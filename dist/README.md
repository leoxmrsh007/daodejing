# 道德经静态网站 - 部署指南

本静态网站无需服务器，可直接部署到任何静态托管平台。

---

## 📁 生成的文件结构

```
dist/
├── index.html              # 首页
├── all-chapters.html       # 全部章节目录
├── chapter1.html           # 第1章
├── chapter2.html           # 第2章
├── ...
├── chapter81.html          # 第81章
└── assets/
    ├── css/
    │   └── style.css       # 样式文件
    └── js/
        └── main.js         # 脚本文件
```

---

## 🚀 部署方案

### 方案一：Gitee Pages（推荐，国内访问最快）

1. **创建Gitee仓库**
   - 访问 https://gitee.com/
   - 创建一个新仓库（如：`daodejing`）

2. **上传文件**
   ```bash
   # 进入dist目录
   cd dist

   # 初始化git仓库
   git init
   git add .
   git commit -m "Initial commit"

   # 添加远程仓库（替换为你的仓库地址）
   git remote add origin https://gitee.com/你的用户名/daodejing.git

   # 推送
   git push -u origin main
   ```

3. **启用Gitee Pages**
   - 进入仓库页面
   - 点击「服务」→「Gitee Pages」
   - 选择部署分支：`main`
   - 点击「启动」
   - 几秒后即可访问：`https://你的用户名.gitee.io/daodejing`

---

### 方案二：Vercel（国外，免费）

1. **安装Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **部署**
   ```bash
   cd dist
   vercel
   ```

3. 按提示完成部署，访问地址类似：`https://daodejing.vercel.app`

---

### 方案三：Netlify（国外，免费）

1. 访问 https://www.netlify.com/

2. 拖拽 `dist` 文件夹到 Netlify 部署页面

3. 几秒后完成，获得免费域名

---

### 方案四：GitHub Pages（国内可能较慢）

1. 创建GitHub仓库

2. 上传dist目录内容

3. 在仓库Settings → Pages中启用：
   - Source: Deploy from a branch
   - Branch: main / root

4. 访问：`https://你的用户名.github.io/仓库名`

---

## 🔄 更新网站

当数据更新后，重新生成静态文件：

```bash
# 在项目根目录执行
python generate_static.py

# 然后部署更新
cd dist
git add .
git commit -m "Update content"
git push
```

---

## 🌐 自定义域名

如果需要使用自己的域名：

1. 在域名DNS设置中添加CNAME记录
2. 在托管平台设置自定义域名
3. 等待DNS生效（通常几分钟到几小时）

---

## 📊 网站特点

- ✅ 纯静态，无需服务器
- ✅ 响应式设计，支持移动端
- ✅ 暗黑模式支持
- ✅ 81章完整内容
- ✅ 多版本对照（王弼、河上公、王夫之、憨山德清）
- ✅ 帛书、郭店楚简异文
- ✅ 英文三译本（D.C. Lau、Henricks、Addiss & Lombardo）
- ✅ 疑难字拼音标注
- ✅ 加载速度快

---

## 📝 文件大小

- 总大小：约 2.1 MB
- HTML文件：83个
- 首次加载：约 50 KB

---

生成时间：{date}
