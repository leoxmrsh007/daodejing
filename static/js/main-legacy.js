(function(){'use strict';const ThemeManager={STORAGE_KEY:'daodejing_theme',ICONS:{light:'☀️',dark:'🌙'},init(){this.themeToggle=document.getElementById('themeToggle');if(!this.themeToggle)return;const savedTheme=localStorage.getItem(this.STORAGE_KEY);if(savedTheme){this.setTheme(savedTheme);}else{this.detectSystemTheme();}
this.themeToggle.addEventListener('click',()=>this.toggle());window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change',()=>{if(!localStorage.getItem(this.STORAGE_KEY)){this.detectSystemTheme();}});},detectSystemTheme(){const prefersDark=window.matchMedia('(prefers-color-scheme: dark)').matches;this.setTheme(prefersDark?'dark':'light');},toggle(){const currentTheme=document.documentElement.getAttribute('data-theme');const newTheme=currentTheme==='dark'?'light':'dark';this.setTheme(newTheme);},setTheme(theme){document.documentElement.setAttribute('data-theme',theme);localStorage.setItem(this.STORAGE_KEY,theme);this.updateIcon();},updateIcon(){const icon=this.themeToggle.querySelector('.theme-icon');const currentTheme=document.documentElement.getAttribute('data-theme');icon.textContent=currentTheme==='dark'?this.ICONS.dark:this.ICONS.light;}};const SidebarManager={init(){this.sidebar=document.getElementById('sidebar');this.toggleBtn=document.getElementById('sidebarToggle');this.overlay=null;if(!this.sidebar||!this.toggleBtn)return;this.createOverlay();this.bindEvents();},createOverlay(){this.overlay=document.createElement('div');this.overlay.className='sidebar-overlay';document.body.appendChild(this.overlay);},bindEvents(){this.toggleBtn.addEventListener('click',()=>this.toggle());this.overlay.addEventListener('click',()=>this.close());const chapterLinks=this.sidebar.querySelectorAll('.chapter-item');chapterLinks.forEach(link=>{link.addEventListener('click',()=>{if(window.innerWidth<992){this.close();}});});window.addEventListener('resize',()=>{if(window.innerWidth>=992){this.sidebar.classList.remove('show');this.overlay.classList.remove('show');}});},toggle(){this.sidebar.classList.toggle('show');this.overlay.classList.toggle('show');},close(){this.sidebar.classList.remove('show');this.overlay.classList.remove('show');}};const SearchManager={API_ENDPOINT:'/api/daodejing/search',DEBOUNCE_DELAY:300,init(){this.searchInput=document.getElementById('searchInput');this.searchModal=document.getElementById('searchModal');this.searchResults=document.getElementById('searchResults');this.debounceTimer=null;if(!this.searchInput)return;this.bindEvents();},bindEvents(){this.searchInput.addEventListener('input',(e)=>{clearTimeout(this.debounceTimer);this.debounceTimer=setTimeout(()=>{this.search(e.target.value);},this.DEBOUNCE_DELAY);});this.searchInput.addEventListener('keydown',(e)=>{if(e.key==='Enter'){clearTimeout(this.debounceTimer);this.search(e.target.value);this.showModal();}});this.searchInput.addEventListener('keydown',(e)=>{if(e.key==='Escape'){this.searchInput.value='';this.searchInput.blur();}});},async search(query){if(!query||query.trim().length<1){return;}
try{const response=await fetch(`${this.API_ENDPOINT}?q=${encodeURIComponent(query)}`);const data=await response.json();this.displayResults(data.results);if(data.results.length>0){this.showModal();}}catch(error){console.error('搜索失败:',error);}},displayResults(results){if(results.length===0){this.searchResults.innerHTML='<p class="text-muted text-center">未找到相关内容</p>';return;}
this.searchResults.innerHTML=results.map(result=>`
                <div class="search-result-item" onclick="location.href='/daodejing/chapter/${result.id}'">
                    <h6 class="mb-1">${result.title}</h6>
                    <p class="small text-muted mb-0">${result.excerpt}</p>
                </div>
            `).join('');},showModal(){if(!this.searchModal)return;const modal=new bootstrap.Modal(this.searchModal);modal.show();}};const MusicManager={STORAGE_KEY:'daodejing_music_volume',DEFAULT_VOLUME:0.3,init(){this.audio=document.getElementById('bgMusic');this.toggleBtn=document.getElementById('musicToggle');this.loopBtn=document.getElementById('musicLoop');this.volumePanel=document.getElementById('volumePanel');this.volumeSlider=document.getElementById('volumeSlider');this.volumeValue=document.getElementById('volumeValue');this.closeVolumeBtn=document.getElementById('closeVolumePanel');if(!this.audio||!this.toggleBtn)return;const savedVolume=localStorage.getItem(this.STORAGE_KEY);this.volume=savedVolume?parseFloat(savedVolume):this.DEFAULT_VOLUME;this.audio.volume=this.volume;if(this.volumeSlider){this.volumeSlider.value=this.volume*100;this.volumeValue.textContent=Math.round(this.volume*100);}
this.bindEvents();},bindEvents(){this.toggleBtn.addEventListener('click',()=>this.toggle());if(this.loopBtn){this.loopBtn.addEventListener('click',()=>this.toggleLoop());}
if(this.volumeSlider){this.volumeSlider.addEventListener('input',(e)=>{this.setVolume(e.target.value/100);});}
if(this.closeVolumeBtn){this.closeVolumeBtn.addEventListener('click',()=>{this.volumePanel.classList.remove('show');});}
document.addEventListener('click',(e)=>{if(this.volumePanel&&this.volumePanel.classList.contains('show')){if(!this.volumePanel.contains(e.target)&&!this.toggleBtn.contains(e.target)){this.volumePanel.classList.remove('show');}}});this.toggleBtn.addEventListener('contextmenu',(e)=>{e.preventDefault();this.volumePanel.classList.toggle('show');});this.audio.addEventListener('play',()=>this.updateState());this.audio.addEventListener('pause',()=>this.updateState());this.audio.addEventListener('ended',()=>this.updateState());},toggle(){if(this.audio.paused){this.audio.play().then(()=>{this.updateState();}).catch(err=>{console.warn('自动播放被阻止，需要用户交互:',err);});}else{this.audio.pause();this.updateState();}},setVolume(value){this.volume=Math.max(0,Math.min(1,value));this.audio.volume=this.volume;localStorage.setItem(this.STORAGE_KEY,this.volume);if(this.volumeValue){this.volumeValue.textContent=Math.round(this.volume*100);}},toggleLoop(){this.audio.loop=!this.audio.loop;this.updateState();},updateState(){const icon=this.toggleBtn.querySelector('.music-icon');if(!this.audio.paused){icon.classList.add('playing');this.toggleBtn.classList.add('active');if(this.loopBtn){this.loopBtn.classList.remove('d-none');}}else{icon.classList.remove('playing');this.toggleBtn.classList.remove('active');if(this.loopBtn){this.loopBtn.classList.add('d-none');}}
if(this.loopBtn&&!this.loopBtn.classList.contains('d-none')){const loopIcon=this.loopBtn.querySelector('.loop-icon');loopIcon.textContent=this.audio.loop?'🔁':'🔂';}}};const SpeechManager={STORAGE_KEY:'daodejing_speech_rate',VOICE_KEY:'daodejing_selected_voice',DEFAULT_RATE:0.9,API_KEY_STORAGE:'daodejing_ai_keys',init(){this.toggleBtn=document.getElementById('speechToggle');this.stopBtn=document.getElementById('speechStop');this.speechPanel=document.getElementById('speechPanel');this.closeSpeechBtn=document.getElementById('closeSpeechPanel');this.speechRate=document.getElementById('speechRate');this.rateValue=document.getElementById('rateValue');this.speechStatus=document.getElementById('speechStatus');this.browserVoiceSelect=document.getElementById('browserVoice');if(!('speechSynthesis'in window)){if(this.toggleBtn){this.toggleBtn.disabled=true;this.toggleBtn.title='您的浏览器不支持朗读功能';}
return;}
const savedRate=localStorage.getItem(this.STORAGE_KEY);this.rate=savedRate?parseFloat(savedRate):this.DEFAULT_RATE;if(this.speechRate){this.speechRate.value=this.rate*100;this.rateValue.textContent=this.rate.toFixed(1);}
this.synth=window.speechSynthesis;this.currentUtterance=null;this.isPaused=false;this.currentChapter=1;this.speechMode='current';this.voices=[];this.selectedVoice=null;this.loadVoices();if(this.synth.onvoiceschanged!==undefined){this.synth.onvoiceschanged=()=>this.loadVoices();}
this.bindEvents();},loadVoices(){this.voices=this.synth.getVoices();const voiceSelect=this.browserVoiceSelect;if(!voiceSelect||this.voices.length===0)return;voiceSelect.innerHTML='';const savedVoiceUri=localStorage.getItem(this.VOICE_KEY);const chineseVoices=this.voices.filter(v=>v.lang.startsWith('zh'));const otherVoices=this.voices.filter(v=>!v.lang.startsWith('zh'));const sortedVoices=[...chineseVoices.filter(v=>v.name.includes('Microsoft')),...chineseVoices.filter(v=>!v.name.includes('Microsoft')),...otherVoices];sortedVoices.forEach((voice,index)=>{const option=document.createElement('option');option.value=index;const displayName=`${voice.name} (${voice.lang})`;option.textContent=displayName;if(voice.name.includes('Microsoft')){option.textContent='⭐ '+displayName;}
voiceSelect.appendChild(option);});if(savedVoiceUri!==null){const savedIndex=sortedVoices.findIndex(v=>v.voiceURI===savedVoiceUri);if(savedIndex!==-1){voiceSelect.value=sortedVoices.indexOf(sortedVoices[savedIndex]);}}
if(voiceSelect.value===""){const defaultIndex=sortedVoices.findIndex(v=>v.name.includes('Microsoft')&&v.lang.startsWith('zh'));if(defaultIndex!==-1){voiceSelect.value=defaultIndex;}}
voiceSelect.removeEventListener('change',this.handleVoiceChange);voiceSelect.addEventListener('change',()=>this.handleVoiceChange());},handleVoiceChange(){const voiceSelect=this.browserVoiceSelect;if(!voiceSelect)return;const sortedVoices=this.getSortedVoices();const index=parseInt(voiceSelect.value);if(sortedVoices[index]){this.selectedVoice=sortedVoices[index];localStorage.setItem(this.VOICE_KEY,this.selectedVoice.voiceURI);console.log('已选择语音:',this.selectedVoice.name,this.selectedVoice.lang);}},getSortedVoices(){const chineseVoices=this.voices.filter(v=>v.lang.startsWith('zh'));const otherVoices=this.voices.filter(v=>!v.lang.startsWith('zh'));return[...chineseVoices.filter(v=>v.name.includes('Microsoft')),...chineseVoices.filter(v=>!v.name.includes('Microsoft')),...otherVoices];},bindEvents(){if(!this.toggleBtn)return;this.toggleBtn.addEventListener('click',()=>this.toggle());if(this.stopBtn){this.stopBtn.addEventListener('click',()=>this.stop());}
if(this.speechRate){this.speechRate.addEventListener('input',(e)=>{this.setRate(e.target.value/100);});}
if(this.closeSpeechBtn){this.closeSpeechBtn.addEventListener('click',()=>{this.speechPanel.classList.remove('show');});}
document.addEventListener('click',(e)=>{if(this.speechPanel&&this.speechPanel.classList.contains('show')){if(!this.speechPanel.contains(e.target)&&!this.toggleBtn.contains(e.target)){this.speechPanel.classList.remove('show');}}});this.toggleBtn.addEventListener('contextmenu',(e)=>{e.preventDefault();this.speechPanel.classList.toggle('show');});const modeInputs=document.querySelectorAll('input[name="speechMode"]');modeInputs.forEach(input=>{input.addEventListener('change',(e)=>{this.speechMode=e.target.value;});});},isUsingFishAudio(){const ttsEngine=localStorage.getItem('daodejing_tts_engine');return ttsEngine==='fish';},getFishAudioConfig(){const saved=localStorage.getItem(this.API_KEY_STORAGE);const apiKeys=saved?JSON.parse(saved):{};return{apiKey:apiKeys.fish||'',voiceId:apiKeys.fishVoiceId||''};},toggle(){if(this.isPlayingFishAudio){if(this.isPaused){this.resume();}else{this.pause();}
return;}
if(this.synth.speaking){if(this.isPaused){this.resume();}else{this.pause();}}else{this.start();}},start(){const originalText=document.getElementById('originalText');if(!originalText){this.setStatus('无法找到原文内容',false);return;}
const text=this.cleanText(originalText.textContent);if(!text){this.setStatus('原文内容为空',false);return;}
const breadcrumb=document.querySelector('.breadcrumb .active');if(breadcrumb){const match=breadcrumb.textContent.match(/第(\d+)章/);if(match){this.currentChapter=parseInt(match[1]);}}
this.speak(text);},speak(text){this.stop();this.speakWithSystem(text);},speakWithSystem(text){this.isPlayingFishAudio=false;this.currentUtterance=new SpeechSynthesisUtterance(text);this.currentUtterance.lang='zh-CN';this.currentUtterance.rate=this.rate;this.currentUtterance.pitch=1;if(this.selectedVoice){this.currentUtterance.voice=this.selectedVoice;}else{const chineseVoice=this.voices.find(v=>v.lang.startsWith('zh')&&v.name.includes('Microsoft'));if(chineseVoice){this.currentUtterance.voice=chineseVoice;this.selectedVoice=chineseVoice;}}
this.currentUtterance.onstart=()=>{this.updateState();const voiceName=this.selectedVoice?this.selectedVoice.name.split(' ').slice(-1)[0]:'系统';this.setStatus(`正在朗读第${this.currentChapter}章 (${voiceName})`,true);};this.currentUtterance.onend=()=>{if(this.speechMode==='all'&&this.currentChapter<81&&!this.isPaused){this.nextChapter();}else{this.updateState();this.setStatus('朗读完成',false);}};this.currentUtterance.onerror=(event)=>{if(event.error!=='interrupted'&&event.error!=='canceled'){console.error('朗读错误:',event.error);this.setStatus('朗读出错: '+event.error,false);}
this.updateState();};this.synth.speak(this.currentUtterance);},pause(){if(this.isPlayingFishAudio&&this.currentAudio){this.currentAudio.pause();this.isPaused=true;this.updateState();this.setStatus('已暂停',false);return;}
if(this.synth.speaking&&!this.isPaused){this.synth.pause();this.isPaused=true;this.updateState();this.setStatus('已暂停',false);}},resume(){if(this.isPlayingFishAudio&&this.currentAudio){this.currentAudio.play();this.isPaused=false;this.updateState();this.setStatus('正在朗读...',true);return;}
if(this.isPaused){this.synth.resume();this.isPaused=false;this.updateState();this.setStatus('正在朗读...',true);}},stop(){if(this.currentAudio){this.currentAudio.pause();this.currentAudio=null;}
this.isPlayingFishAudio=false;this.synth.cancel();this.isPaused=false;this.updateState();this.setStatus('已停止',false);},nextChapter(){this.currentChapter++;const nextLink=document.querySelector(`.chapter-item[data-chapter="${this.currentChapter}"]`);if(nextLink){nextLink.click();setTimeout(()=>{this.start();},500);}},setRate(value){this.rate=Math.max(0.5,Math.min(1.5,value));localStorage.setItem(this.STORAGE_KEY,this.rate);if(this.rateValue){this.rateValue.textContent=this.rate.toFixed(1);}},cleanText(text){return text.replace(/\s+/g,'').replace(/([，。；：！？、])/g,'$1 ').trim();},setStatus(text,isActive){if(this.speechStatus){this.speechStatus.textContent=text;if(isActive){this.speechStatus.classList.add('active');}else{this.speechStatus.classList.remove('active');}}},updateState(){const icon=this.toggleBtn.querySelector('.speech-icon');const isSpeaking=this.synth.speaking&&!this.isPaused;if(isSpeaking){icon.classList.add('speaking');this.toggleBtn.classList.add('active');if(this.stopBtn){this.stopBtn.classList.remove('d-none');}}else{icon.classList.remove('speaking');this.toggleBtn.classList.remove('active');if(this.stopBtn){this.stopBtn.classList.add('d-none');}}}};const ScrollHighlight={init(){this.sidebar=document.getElementById('sidebar');this.chapterItems=document.querySelectorAll('.chapter-item');if(this.chapterItems.length===0)return;this.setupObserver();},setupObserver(){const options={root:null,rootMargin:'-20% 0px -60% 0px',threshold:0};this.observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){const id=entry.target.dataset.chapter;this.highlightChapter(id);}});},options);document.querySelectorAll('.original-text, .original-section').forEach(el=>{this.observer.observe(el);});},highlightChapter(chapterId){this.chapterItems.forEach(item=>{item.classList.remove('active');if(item.dataset.chapter==chapterId){item.classList.add('active');item.scrollIntoView({behavior:'smooth',block:'nearest'});}});}};const SettingsManager={STORAGE_KEY:'daodejing_settings',defaults:{mode:'reading',font:'default',fontSize:'medium',textLayout:'center',musicType:'none',musicVolume:30,showPinyin:true,showAnnotation:true,showModern:true,showNotes:true,showEnglish:false},settings:{},currentMusicIndex:0,init(){this.settingsBtn=document.getElementById('settingsToggle');this.settingsPanel=document.getElementById('settingsPanel');this.closeSettingsBtn=document.getElementById('closeSettingsPanel');this.zenOverlay=document.getElementById('zenModeOverlay');this.zenExitBtn=document.getElementById('zenExitBtn');if(!this.settingsBtn)return;this.loadSettings();this.loadFromURL();this.applySettings();this.bindEvents();},bindEvents(){this.settingsBtn.addEventListener('click',()=>this.togglePanel());if(this.closeSettingsBtn){this.closeSettingsBtn.addEventListener('click',()=>this.closePanel());}
document.addEventListener('click',(e)=>{if(this.settingsPanel&&this.settingsPanel.classList.contains('show')){if(!this.settingsPanel.contains(e.target)&&!this.settingsBtn.contains(e.target)){this.closePanel();}}});const modeBtns=this.settingsPanel?.querySelectorAll('.mode-btn');modeBtns?.forEach(btn=>{btn.addEventListener('click',()=>{const mode=btn.dataset.mode;this.setMode(mode);});});const fontSelect=document.getElementById('fontSelect');if(fontSelect){fontSelect.addEventListener('change',(e)=>{this.setFont(e.target.value);});}
const sizeBtns=this.settingsPanel?.querySelectorAll('.size-btn');sizeBtns?.forEach(btn=>{btn.addEventListener('click',()=>{const size=btn.dataset.size;this.setFontSize(size);});});const layoutBtns=this.settingsPanel?.querySelectorAll('.layout-btn');layoutBtns?.forEach(btn=>{btn.addEventListener('click',()=>{const layout=btn.dataset.layout;this.setTextLayout(layout);});});const musicSelect=document.getElementById('musicSelect');if(musicSelect){musicSelect.addEventListener('change',(e)=>{this.setMusicType(e.target.value);});}
const volumeSlider=document.getElementById('musicVolumeSlider');const volumeValue=document.getElementById('musicVolumeValue');if(volumeSlider){volumeSlider.addEventListener('input',(e)=>{this.setMusicVolume(e.target.value);if(volumeValue){volumeValue.textContent=e.target.value+'%';}});}
const showPinyin=document.getElementById('showPinyin');const showAnnotation=document.getElementById('showAnnotation');if(showPinyin){showPinyin.addEventListener('change',(e)=>{this.setShowPinyin(e.target.checked);});}
if(showAnnotation){showAnnotation.addEventListener('change',(e)=>{this.setShowAnnotation(e.target.checked);});}
const showModern=document.getElementById('showModern');const showNotes=document.getElementById('showNotes');const showEnglish=document.getElementById('showEnglish');if(showModern){showModern.addEventListener('change',(e)=>{this.setShowModern(e.target.checked);});}
if(showNotes){showNotes.addEventListener('change',(e)=>{this.setShowNotes(e.target.checked);});}
if(showEnglish){showEnglish.addEventListener('change',(e)=>{this.setShowEnglish(e.target.checked);});}
if(this.zenExitBtn){this.zenExitBtn.addEventListener('click',()=>{this.exitZenMode();});}
document.addEventListener('keydown',(e)=>{if(e.key==='Escape'){if(this.zenOverlay?.classList.contains('active')){this.exitZenMode();}else if(this.settingsPanel?.classList.contains('show')){this.closePanel();}}});const shareBtn=document.getElementById('shareSettings');if(shareBtn){shareBtn.addEventListener('click',()=>this.shareSettings());}},togglePanel(){this.settingsPanel.classList.toggle('show');},closePanel(){this.settingsPanel.classList.remove('show');},loadSettings(){const saved=localStorage.getItem(this.STORAGE_KEY);if(saved){try{this.settings={...this.defaults,...JSON.parse(saved)};}catch(e){this.settings={...this.defaults};}}else{this.settings={...this.defaults};}},saveSettings(){localStorage.setItem(this.STORAGE_KEY,JSON.stringify(this.settings));},loadFromURL(){const params=new URLSearchParams(window.location.search);const config=params.get('config');if(config){try{const urlSettings=JSON.parse(atob(config));this.settings={...this.settings,...urlSettings};this.saveSettings();}catch(e){console.error('解析URL配置失败:',e);}}},applySettings(){this.setMode(this.settings.mode,false);this.setFont(this.settings.font,false);this.setFontSize(this.settings.fontSize,false);this.setTextLayout(this.settings.textLayout,false);this.setMusicType(this.settings.musicType,false);this.setMusicVolume(this.settings.musicVolume,false);this.setShowPinyin(this.settings.showPinyin,false);this.setShowAnnotation(this.settings.showAnnotation,false);this.setShowModern(this.settings.showModern,false);this.setShowNotes(this.settings.showNotes,false);this.setShowEnglish(this.settings.showEnglish,false);this.updateUIState();},updateUIState(){const modeBtns=this.settingsPanel?.querySelectorAll('.mode-btn');modeBtns?.forEach(btn=>{btn.classList.toggle('active',btn.dataset.mode===this.settings.mode);});const fontSelect=document.getElementById('fontSelect');if(fontSelect){fontSelect.value=this.settings.font;}
const sizeBtns=this.settingsPanel?.querySelectorAll('.size-btn');sizeBtns?.forEach(btn=>{btn.classList.toggle('active',btn.dataset.size===this.settings.fontSize);});const layoutBtns=this.settingsPanel?.querySelectorAll('.layout-btn');layoutBtns?.forEach(btn=>{btn.classList.toggle('active',btn.dataset.layout===this.settings.textLayout);});const musicSelect=document.getElementById('musicSelect');if(musicSelect){musicSelect.value=this.settings.musicType;}
const volumeSlider=document.getElementById('musicVolumeSlider');const volumeValue=document.getElementById('musicVolumeValue');if(volumeSlider){volumeSlider.value=this.settings.musicVolume;}
if(volumeValue){volumeValue.textContent=this.settings.musicVolume+'%';}
const showPinyin=document.getElementById('showPinyin');const showAnnotation=document.getElementById('showAnnotation');const showModern=document.getElementById('showModern');const showNotes=document.getElementById('showNotes');const showEnglish=document.getElementById('showEnglish');if(showPinyin)showPinyin.checked=this.settings.showPinyin;if(showAnnotation)showAnnotation.checked=this.settings.showAnnotation;if(showModern)showModern.checked=this.settings.showModern;if(showNotes)showNotes.checked=this.settings.showNotes;if(showEnglish)showEnglish.checked=this.settings.showEnglish;},setMode(mode,save=true){this.settings.mode=mode;if(save)this.saveSettings();const body=document.body;body.classList.remove('mode-reading','mode-zen','mode-recite');body.classList.add(`mode-${mode}`);if(mode==='zen'){this.enterZenMode();}else{this.exitZenMode();}
if(mode==='recite'){body.classList.add('hide-modern','hide-notes','hide-english');}else{body.classList.remove('hide-modern','hide-notes','hide-english');}
this.updateUIState();},setFont(font,save=true){this.settings.font=font;if(save)this.saveSettings();const body=document.body;body.classList.remove('font-default','font-kaiti','font-songti','font-fangsong','font-mingliu','font-xkai');body.classList.add(`font-${font}`);this.updateUIState();},setFontSize(size,save=true){this.settings.fontSize=size;if(save)this.saveSettings();const body=document.body;body.classList.remove('font-size-small','font-size-medium','font-size-large');body.classList.add(`font-size-${size}`);this.updateUIState();},setTextLayout(layout,save=true){this.settings.textLayout=layout;if(save)this.saveSettings();const body=document.body;body.classList.remove('text-layout-center','text-layout-left');body.classList.add(`text-layout-${layout}`);this.updateUIState();},setMusicType(type,save=true){this.settings.musicType=type;if(save)this.saveSettings();const audio=document.getElementById('bgMusic');if(!audio)return;const wasPlaying=!audio.paused;audio.pause();if(type==='none'){audio.removeAttribute('src');this.updateUIState();return;}
const tracks=window.musicTracks?.[type]||[];if(tracks.length===0)return;this.currentMusicIndex=Math.floor(Math.random()*tracks.length);audio.src=tracks[this.currentMusicIndex];audio.load();if(wasPlaying){audio.play().catch(err=>{console.warn('自动播放被阻止:',err);});}
this.updateUIState();},setMusicVolume(volume,save=true){this.settings.musicVolume=parseInt(volume);if(save)this.saveSettings();const audio=document.getElementById('bgMusic');if(audio){audio.volume=this.settings.musicVolume/100;}},setShowPinyin(show,save=true){this.settings.showPinyin=show;if(save)this.saveSettings();const body=document.body;if(show){body.classList.remove('hide-pinyin');}else{body.classList.add('hide-pinyin');}
this.updateUIState();},setShowAnnotation(show,save=true){this.settings.showAnnotation=show;if(save)this.saveSettings();const body=document.body;if(show){body.classList.remove('hide-annotation');}else{body.classList.add('hide-annotation');}
this.updateUIState();},setShowModern(show,save=true){this.settings.showModern=show;if(save)this.saveSettings();const body=document.body;if(show){body.classList.remove('hide-modern');}else{body.classList.add('hide-modern');}
this.updateUIState();},setShowNotes(show,save=true){this.settings.showNotes=show;if(save)this.saveSettings();const body=document.body;if(show){body.classList.remove('hide-notes');}else{body.classList.add('hide-notes');}
this.updateUIState();},setShowEnglish(show,save=true){this.settings.showEnglish=show;if(save)this.saveSettings();const body=document.body;if(show){body.classList.remove('hide-english');}else{body.classList.add('hide-english');}
this.updateUIState();},enterZenMode(){const originalText=document.querySelector('.original-text');if(!originalText)return;const content=originalText.innerHTML;this.zenOverlay.innerHTML=`
                <button class="zen-exit-btn" id="zenExitBtn">退出禅读</button>
                <div class="zen-content">
                    <div class="original-text">${content}</div>
                </div>
            `;this.zenOverlay.querySelector('#zenExitBtn').addEventListener('click',()=>{this.exitZenMode();});this.zenOverlay.classList.add('active');document.body.style.overflow='hidden';},exitZenMode(){this.zenOverlay.classList.remove('active');document.body.style.overflow='';if(this.settings.mode==='zen'){}},shareSettings(){const config=btoa(JSON.stringify(this.settings));const url=`${window.location.origin}${window.location.pathname}?config=${config}`;navigator.clipboard.writeText(url).then(()=>{const shareBtn=document.getElementById('shareSettings');const originalText=shareBtn.innerHTML;shareBtn.innerHTML='<span>✓</span> 已复制链接';setTimeout(()=>{shareBtn.innerHTML=originalText;},2000);}).catch(()=>{alert('分享链接：'+url);});}};const ShareManager={init(){this.shareBtn=document.getElementById('shareToggle');this.supportBtn=document.getElementById('supportBtn');this.communityBtn=document.getElementById('communityBtn');this.shareModal=document.getElementById('shareModal');this.shareUrlInput=document.getElementById('shareUrlInput');if(!this.shareBtn)return;this.bindEvents();},bindEvents(){this.shareBtn?.addEventListener('click',()=>this.openShareModal());this.supportBtn?.addEventListener('click',()=>{const modal=new bootstrap.Modal(document.getElementById('supportModal'));modal.show();});this.communityBtn?.addEventListener('click',()=>{const modal=new bootstrap.Modal(document.getElementById('communityModal'));modal.show();});document.getElementById('shareWechat')?.addEventListener('click',()=>{this.shareToWechat();});document.getElementById('shareWeibo')?.addEventListener('click',()=>{this.shareToWeibo();});document.getElementById('shareLink')?.addEventListener('click',()=>{this.copyLink();});},openShareModal(){if(!this.shareModal)return;const shareUrl=window.location.href.split('?')[0];if(this.shareUrlInput){this.shareUrlInput.value=shareUrl;}
const modal=new bootstrap.Modal(this.shareModal);modal.show();},shareToWechat(){const shareUrl=window.location.href.split('?')[0];const title='道德经多版本对照阅读平台';const text=`《道德经》81章完整版，支持王弼、河上公、王夫之、帛书、楚简多版本对照，疑难字注音，暗黑模式。`;alert(`请复制链接在微信中分享：\n${shareUrl}\n\n${text}`);},shareToWeibo(){const shareUrl=encodeURIComponent(window.location.href.split('?')[0]);const title=encodeURIComponent('道德经多版本对照阅读平台 - 王弼·河上公·王夫之·帛书·英文译本');const text=encodeURIComponent('《道德经》81章完整版，支持多版本对照，疑难字注音，暗黑模式，手机阅读。');window.open(`https://service.weibo.com/share/share.php?url=${shareUrl}&title=${title}&pic=`,'_blank');},copyLink(){const shareUrl=window.location.href.split('?')[0];const title='道德经多版本对照阅读平台';const text=`《道德经》81章完整版，支持王弼、河上公、王夫之、帛书、楚简多版本对照。`;navigator.clipboard.writeText(`${title}\n${text}\n${shareUrl}`).then(()=>{const shareBtn=document.getElementById('shareLink');const originalHTML=shareBtn.innerHTML;shareBtn.innerHTML='<span class="share-icon">✓</span><span>已复制</span>';setTimeout(()=>{shareBtn.innerHTML=originalHTML;},2000);}).catch(()=>{const textArea=document.createElement('textarea');textArea.value=`${title}\n${text}\n${shareUrl}`;document.body.appendChild(textArea);textArea.select();document.execCommand('copy');document.body.removeChild(textArea);alert('链接已复制！');});}};const AIManager={API_KEY_STORAGE:'daodejing_ai_keys',messages:[],isGenerating:false,init(){this.aiToggle=document.getElementById('aiToggle');this.aiSidebar=document.getElementById('aiSidebar');this.aiOverlay=document.getElementById('aiOverlay');this.aiCloseSidebar=document.getElementById('aiCloseSidebar');this.aiNewChat=document.getElementById('aiNewChat');this.aiMessages=document.getElementById('aiMessages');this.aiInput=document.getElementById('aiInput');this.aiSend=document.getElementById('aiSend');this.aiModel=document.getElementById('aiModel');this.aiSuggestions=document.getElementById('aiSuggestions');this.bindEvents();this.loadApiKeys();},bindEvents(){this.aiToggle?.addEventListener('click',()=>this.toggleSidebar());this.aiCloseSidebar?.addEventListener('click',()=>this.closeSidebar());this.aiOverlay?.addEventListener('click',()=>this.closeSidebar());this.aiNewChat?.addEventListener('click',()=>this.newChat());this.aiSend?.addEventListener('click',()=>this.sendMessage());this.aiInput?.addEventListener('keydown',(e)=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();this.sendMessage();}});this.aiSuggestions?.querySelectorAll('.ai-suggestion-btn').forEach(btn=>{btn.addEventListener('click',()=>{const question=btn.dataset.question;this.aiInput.value=question;this.sendMessage();});});},toggleSidebar(){this.aiSidebar.classList.toggle('show');this.aiOverlay.classList.toggle('show',this.aiSidebar.classList.contains('show'));},closeSidebar(){this.aiSidebar.classList.remove('show');this.aiOverlay.classList.remove('show');},newChat(){this.messages=[];this.updateMessagesDisplay();this.showWelcome();},showWelcome(){if(this.aiMessages){this.aiMessages.innerHTML=`
                    <div class="ai-welcome">
                        <div class="ai-welcome-icon">🤖</div>
                        <h6>道德经AI助手</h6>
                        <p>您可以：</p>
                        <ul>
                            <li>点击下方快捷问题开始</li>
                            <li>或直接输入您的问题</li>
                        </ul>
                    </div>
                `;}},async sendMessage(){const question=this.aiInput?.value.trim();if(!question||this.isGenerating)return;const welcome=this.aiMessages?.querySelector('.ai-welcome');if(welcome)welcome.remove();this.messages.push({role:'user',content:question});this.addMessageToDisplay('user',question);this.aiInput.value='';this.aiSuggestions?.querySelectorAll('.ai-suggestion-btn').forEach(btn=>{btn.style.display='none';});this.showTyping();const chapterContent=this.getChapterContent();const response=await this.callAI(question,chapterContent);this.hideTyping();this.messages.push({role:'assistant',content:response});this.addMessageToDisplay('assistant',response);},addMessageToDisplay(role,content){const messageDiv=document.createElement('div');messageDiv.className=`ai-message ${role}`;if(role==='user'){messageDiv.innerHTML=`
                    <div class="ai-message-content">${this.escapeHtml(content)}</div>
                `;}else{messageDiv.innerHTML=`
                    <div class="ai-message-header">🤖 道德经AI助手</div>
                    <div class="ai-message-content">${this.formatContent(content)}</div>
                `;}
this.aiMessages.appendChild(messageDiv);this.scrollToBottom();},showTyping(){const typingDiv=document.createElement('div');typingDiv.className='ai-message assistant';typingDiv.id='aiTyping';typingDiv.innerHTML=`
                <div class="ai-typing">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            `;this.aiMessages.appendChild(typingDiv);this.scrollToBottom();},hideTyping(){const typing=document.getElementById('aiTyping');if(typing)typing.remove();},updateMessagesDisplay(){this.aiMessages.innerHTML='';this.messages.forEach(msg=>{this.addMessageToDisplay(msg.role,msg.content);});if(this.messages.length===0){this.showWelcome();}},scrollToBottom(){this.aiMessages.scrollTop=this.aiMessages.scrollHeight;},getChapterContent(){const originalText=document.querySelector('.original-text');const chapterTitle=document.querySelector('.breadcrumb .active')?.textContent||'当前章节';return{title:chapterTitle,content:originalText?.textContent||''};},async callAI(question,chapterContent){const model=this.aiModel?.value||'auto';const prompt=this.buildPrompt(question,chapterContent);let response='';if(model==='deepseek'||model==='auto'){const deepseekKey=this.apiKeys?.deepseek;if(deepseekKey){response=await this.callDeepSeek(prompt,deepseekKey);if(response)return response;}}
if(model==='openai'||model==='auto'){const openaiKey=this.apiKeys?.openai;if(openaiKey){response=await this.callOpenAI(prompt,openaiKey);if(response)return response;}}
return this.getFallbackResponse(question,chapterContent);},buildPrompt(question,chapterContent){return`你是《道德经》的解读助手，请基于以下内容回答问题。

【章节】${chapterContent.title}
【原文】${chapterContent.content}

【问题】${question}

请用简洁、通俗易懂的语言回答，突出道德经的智慧和现代应用价值。`;},async callDeepSeek(prompt,apiKey){try{const response=await fetch('https://api.deepseek.com/chat/completions',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${apiKey}`},body:JSON.stringify({model:'deepseek-chat',messages:[{role:'user',content:prompt}],stream:false})});if(!response.ok){throw new Error('API请求失败');}
const data=await response.json();return data.choices[0]?.message?.content||'';}catch(error){console.error('DeepSeek API错误:',error);return'';}},async callOpenAI(prompt,apiKey){try{const response=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${apiKey}`},body:JSON.stringify({model:'gpt-3.5-turbo',messages:[{role:'user',content:prompt}],stream:false})});if(!response.ok){throw new Error('API请求失败');}
const data=await response.json();return data.choices[0]?.message?.content||'';}catch(error){console.error('OpenAI API错误:',error);return'';}},getFallbackResponse(question,chapterContent){const responses={'解读本章核心思想':`根据"${chapterContent.title}"的内容，本章的核心思想是...\n\n💡 要使用AI解读功能，请在设置中配置API Key（DeepSeek或OpenAI）\n\n配置后可获得更深入的AI解读和个性化回答。`,'本章在现代生活中的应用':`道德经的智慧在现代生活中有很多应用...\n\n💡 配置API Key后可获得更详细的应用案例解读`,'解释疑难词句的含义':`本章中的疑难词句包含丰富的哲学内涵...\n\n💡 配置API Key后可获得专业的词语解释`};return responses[question]||`感谢您的问题：「${question}」\n\n💡 要获得AI智能回答，请在设置中配置API Key：\n\n1. DeepSeek API (推荐，价格优惠)\n2. OpenAI API (GPT-3.5)\n\nAPI Key仅存储在您本地浏览器中，安全可靠。`;},formatContent(content){return content.replace(/\n\n/g,'</p><p>').replace(/\*\*(.*?)\*\*/g,'<strong>$1</strong>').replace(/`([^`]+)`/g,'<code>$1</code>');},escapeHtml(text){const div=document.createElement('div');div.textContent=text;return div.innerHTML;},loadApiKeys(){const saved=localStorage.getItem(this.API_KEY_STORAGE);this.apiKeys=saved?JSON.parse(saved):{deepseek:'',openai:'',fish:''};const deepseekInput=document.getElementById('deepseekKey');const openaiInput=document.getElementById('openaiKey');if(deepseekInput)deepseekInput.value=this.apiKeys.deepseek||'';if(openaiInput)openaiInput.value=this.apiKeys.openai||'';deepseekInput?.addEventListener('change',(e)=>{this.apiKeys.deepseek=e.target.value;this.saveApiKeys();});openaiInput?.addEventListener('change',(e)=>{this.apiKeys.openai=e.target.value;this.saveApiKeys();});const fishApiKeyInput=document.getElementById('fishApiKey');const fishVoiceIdInput=document.getElementById('fishVoiceId');if(fishApiKeyInput)fishApiKeyInput.value=this.apiKeys.fish||'';if(fishVoiceIdInput)fishVoiceIdInput.value=this.apiKeys.fishVoiceId||'';fishApiKeyInput?.addEventListener('change',(e)=>{this.apiKeys.fish=e.target.value;this.saveApiKeys();});fishVoiceIdInput?.addEventListener('change',(e)=>{this.apiKeys.fishVoiceId=e.target.value;this.saveApiKeys();});const ttsEngineSelect=document.getElementById('ttsEngine');const fishSettings=document.getElementById('fishAudioSettings');const edgeSettings=document.getElementById('edgeAudioSettings');const savedTtsEngine=localStorage.getItem('daodejing_tts_engine');if(ttsEngineSelect&&savedTtsEngine){ttsEngineSelect.value=savedTtsEngine;}
const updateSettingsPanel=()=>{if(!ttsEngineSelect)return;const engine=ttsEngineSelect.value;if(fishSettings)fishSettings.classList.remove('show');if(edgeSettings)edgeSettings.classList.remove('show');if(engine==='fish'&&fishSettings){fishSettings.classList.add('show');}else if(engine==='edge'&&edgeSettings){edgeSettings.classList.add('show');}};updateSettingsPanel();if(ttsEngineSelect){ttsEngineSelect.addEventListener('change',(e)=>{localStorage.setItem('daodejing_tts_engine',e.target.value);updateSettingsPanel();});}
const savedEdgeVoice=localStorage.getItem('daodejing_edge_voice');const edgeVoiceSelect=document.getElementById('edgeVoice');if(edgeVoiceSelect&&savedEdgeVoice){edgeVoiceSelect.value=savedEdgeVoice;}
if(edgeVoiceSelect){edgeVoiceSelect.addEventListener('change',(e)=>{localStorage.setItem('daodejing_edge_voice',e.target.value);});}},saveApiKeys(){localStorage.setItem(this.API_KEY_STORAGE,JSON.stringify(this.apiKeys));}};document.addEventListener('DOMContentLoaded',()=>{ThemeManager.init();SidebarManager.init();SearchManager.init();MusicManager.init();SpeechManager.init();ScrollHighlight.init();SettingsManager.init();ShareManager.init();AIManager.init();});})();